# Goal Progress

## 已完成

- **B-4 Tool Card 产品化展示**
  - `src/modules/tool-registry.ts`: 扩展 `ToolDefinition`，新增 `purpose`、`scope`、`riskReason` 字段，为全部 12 个工具注册产品化元数据
  - `src/modules/tool-runs.ts`: 新增专属摘要函数 `summarizeProjectMap`、`summarizeGitDiff`、`summarizeGitStatus`、`summarizeGitLog`、`summarizeReadManyFiles`，解析 raw text 产出结构化摘要
  - `src/modules/tool-card.ts`: 重写 `renderToolCard`，默认视图展示：工具名称（带图标）、目的、范围、风险等级+原因、结果摘要、下一步操作；高级详情折叠区包含：raw JSON、token 消耗、耗时、trace id、完整输入/输出
  - `src/modules/evidence-panel.ts`: 集成新的证据展示，默认显示产品化摘要，原始输出可折叠查看
  - `src/styles/tool-card.css`: 全新产品化样式，适配 dark/light 主题，包含风险/状态徽章、行动按钮、折叠动画
  - 交互支持：复制结果、查看证据、继续追问（触发 CustomEvent）
  - 验收：tool-card / tool-runs / tool-registry / tools 测试全部通过（85/85），TypeScript strict 检查通过，构建通过

## 进行中

- Phase 1: B-1 输入区降噪 + B-4 Tool Card 产品化（并行）
  - B-4 已完成，等待 B-1

## 关键决策

- CSS 方案：继续使用现有 CSS，不引入新框架
- index.html 分区：B-1 改 composer 区（底部），B-2 改 settings 区（中间），B-3 改 right-panel 区（右侧），互不重叠
- 上下文压缩：每完成一个 Phase 主动压缩

## 当前阻塞

- 无
- 注：`tests/composer-layout.test.ts` 存在 1 个预先存在的失败（`composer-tool-drawer-btn` 缺失），与 B-4 无关

## 下一步

- B-1: 重组 composer-toolbar，折叠高级选项
